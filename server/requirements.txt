# AgentSeed MCP server external dependencies.
# jsonschema — replaces the former hand-rolled JSON Schema subset validator.
#              Floor is the newest release that still installs on Python 3.9
#              (our supported minimum): 4.26+ requires >=3.10, so raising it
#              further would make the 3.9 jobs uninstallable by construction
#              — see the ignore rule in .github/dependabot.yml.
# pyflakes  — optional, used by symbols.py for more accurate Python AST analysis.
# pyyaml    — optional, full YAML frontmatter parsing (lite fallback built in).
jsonschema>=4.25.1
pyflakes>=3.4.0
pyyaml>=6.0.3
